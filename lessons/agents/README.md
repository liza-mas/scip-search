# Agent Lessons

Operational lessons from project experience. Read when a trigger matches.

| Trigger | File |
|---------|------|
| When a Liza state-mutating CLI command returns an internal error from a task worktree | [liza-cli-project-root.md](liza-cli-project-root.md) |
| When an RTK-prefixed `test -d` worktree check fails with `Illegal option -d` | [rtk-test-directory-check.md](rtk-test-directory-check.md) |
